apiVersion: v1
kind: Service
metadata:
  name: devhub-gateway
  namespace: devhub
spec:
  type: NodePort
  selector:
    app: devhub-gateway
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
